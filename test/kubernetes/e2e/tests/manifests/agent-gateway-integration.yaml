image:
  registry: ghcr.io/kgateway-dev

# Configuration for the kgateway controller pod
controller:
  logLevel: debug

gateway:
  # Set up kgateway with agent gateway integration enabled
  agentGatewayIntegration:
    enabled: true